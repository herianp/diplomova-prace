# Technology Stack

**Analysis Date:** 2026-02-14

## Languages

**Primary:**
- TypeScript - Used for service layers, composables, stores, and configuration
- JavaScript - Used for router, boot files, and i18n configuration
- Vue 3 - Used for all component files (.vue)

**Secondary:**
- SCSS - Used for styling in `src/css/` directory

## Runtime

**Environment:**
- Node.js (compatible with versions 28, 26, 24, 22, 20, 18 per package.json)

**Package Manager:**
- Yarn (preferred per package.json) or npm
- Lockfile: `yarn.lock` present (primary), `package-lock.json` also present

## Frameworks

**Core:**
- Vue.js 3.4.18 - Frontend framework for component-based UI
- Quasar Framework 2.16.0 - Vue UI component framework providing Material Design components
- Quasar CLI with Vite - Build system and development server

**State Management:**
- Pinia 2.0.11 - State management library (Setup Store pattern used in `src/stores/authStore.ts` and `src/stores/teamStore.ts`)

**Routing:**
- Vue Router 4.0.0 - Client-side routing

**Testing:**
- Vitest 4.0.18 - Unit test runner
- Happy-DOM 20.5.0 - DOM environment for testing
- Vue Test Utils 2.4.6 - Vue component testing utilities

**Build/Dev:**
- Vite (via @quasar/app-vite 2.0.0) - Build tool and dev server
- ESLint 9.39.2 with @eslint/js 9.14.0 - Code linting
- Prettier 3.3.3 - Code formatting
- eslint-plugin-vue 9.30.0 - Vue-specific ESLint rules
- @vue/eslint-config-prettier 10.1.0 - ESLint Prettier integration
- vite-plugin-checker 0.8.0 - Vite plugin for real-time type checking

**Internationalization:**
- Vue i18n 9.14.5 - Multi-language support
- @intlify/unplugin-vue-i18n 2.0.0 - Vite plugin for i18n pre-processing

**Data Visualization:**
- Chart.js 4.5.0 - Charting library
- vue-chartjs 5.3.2 - Vue wrapper for Chart.js

**Date/Time:**
- Luxon 3.6.0 - Date and time manipulation

**Styling:**
- Bootstrap (referenced in memory but not in current package.json)
- Material Icons (via @quasar/extras 1.16.4) - Icon font
- Roboto Font (via @quasar/extras 1.16.4) - Font family
- AutoPrefix 10.4.2 - CSS vendor prefix handling
- PostCSS 8.4.14 - CSS processing

**Development Utilities:**
- Globals 15.12.0 - Global variable definitions for ESLint

## Key Dependencies

**Critical:**
- firebase 11.4.0 - Backend services: Authentication, Firestore database, Analytics
- vue-i18n 9.14.5 - Multi-language support (Czech/English)
- quasar 2.16.0 - Component library and Material Design system
- pinia 2.0.11 - Application state management

**Infrastructure:**
- @quasar/app-vite 2.0.0 - Quasar build system with Vite
- vite-plugin-checker 0.8.0 - Build-time type and lint checking

## Configuration

**Environment:**
- `.env` file required (git-ignored)
- See `.env.example` for template
- Environment variables accessed via `import.meta.env.VITE_*`

**Required Environment Variables:**
- `VITE_FIREBASE_API_KEY` - Firebase API key
- `VITE_FIREBASE_AUTH_DOMAIN` - Firebase auth domain
- `VITE_FIREBASE_PROJECT_ID` - Firebase project ID
- `VITE_FIREBASE_STORAGE_BUCKET` - Firebase storage bucket
- `VITE_FIREBASE_MESSAGING_SENDER_ID` - Firebase messaging sender ID
- `VITE_FIREBASE_APP_ID` - Firebase app ID
- `VITE_FIREBASE_MEASUREMENT_ID` - Firebase analytics ID

**Build Configuration Files:**
- `quasar.config.js` - Main Quasar configuration
- `vite.config.ts` - Vite build configuration (generated by Quasar)
- `.quasar/tsconfig.json` - Generated TypeScript configuration for Quasar
- `tsconfig.json` - TypeScript configuration (not present in root, uses Quasar's)
- `.eslintrc.js` / `eslint.config.js` - ESLint flat config
- `.prettierrc.json` - Prettier code formatting rules
- `.editorconfig` - Editor configuration
- `.firebaserc` - Firebase CLI configuration (project: "club-surveys")
- `.npmrc` - npm configuration

**Prettier Settings:**
- No semicolons (`semi: false`)
- Single quotes (`singleQuote: true`)
- Line width of 100 characters (`printWidth: 100`)

**ESLint Configuration:**
- Flat config format (eslint.config.js)
- Extends Quasar recommended rules
- Vue 3 essential rules enabled
- Prettier integration for formatting consistency
- Browser and Node globals configured
- Service Worker globals for PWA support

## Platform Requirements

**Development:**
- Node.js 18+ recommended
- npm >= 6.13.4 or yarn >= 1.21.1
- Git for version control

**Production:**
- Browser support: ES2022 target, Firefox 115+, Chrome 115+, Safari 14+
- Hash-based routing for SPA deployment (no server-side routing required)
- Static file hosting capable (no backend server required, Firebase handles backend)

**Firebase Project:**
- Project ID: `club-surveys`
- Region: Not specified (uses Firebase default regions)

## Build Output

**Target Architecture:**
- Modern browsers (ES2022)
- Single Page Application (SPA) with hash routing
- Source maps disabled in production for security
- Code minification enabled
- Build type: Modern mode

**Optimization:**
- Manual chunk splitting configured:
  - `firebase` chunk: Firebase SDK modules
  - `charts` chunk: Chart.js library
  - `vue` chunk: Vue core libraries
  - `quasar` chunk: Quasar framework
  - `luxon` chunk: Date handling library
- Chunk size warning limit: 1000 KB

---

*Stack analysis: 2026-02-14*
