# Plan 23-01: Move Team Members Below Deletion Section

## Goal
Reorder the SingleTeamPage layout so team members appear at the bottom, below the danger zone / delete section.

## Current Order (lines 13-160)
1. **TeamPlayerCardsComponent** + **TeamInvitationComponent** (members grid + invite) — lines 13-40
2. **Team Settings** card (chat, voting cutoff, address) — lines 43-138
3. **Danger Zone** delete card — lines 140-160

## Target Order
1. Team Settings card
2. Danger Zone delete card
3. TeamPlayerCardsComponent + TeamInvitationComponent (members grid + invite)

## Changes

### File: `src/pages/SingleTeamPage.vue`

**Task 1: Move the members/invitation row block (lines 13-40) below the danger zone block (after line 160)**

Cut the entire `<div class="row q-col-gutter-lg">` block containing TeamPlayerCardsComponent and TeamInvitationComponent, and paste it after the danger zone `</div>` (line 160), adding `q-mt-lg` for spacing.

## Files Modified
- `src/pages/SingleTeamPage.vue` (template reorder only)
